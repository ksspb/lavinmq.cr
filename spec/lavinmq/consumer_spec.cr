require "../spec_helper"

describe Lavinmq::Consumer do
  pending "subscribes to queue (requires server)"
  pending "acknowledges messages (requires server)"
  pending "can be closed (requires server)"
end
